// import { createAsyncThunk } from "@reduxjs/toolkit";
// import * as api from "../../services/api";

// const API_ENDPOINT = "contacts";

// const getContacts = createAsyncThunk("contacts/getUtterContactsStatus", token => api.getUtterContacts(API_ENDPOINT, token));

// const addContact = createAsyncThunk("contacts/addContactStatus", (newContact, token) => api.saveContact(API_ENDPOINT, newContact, token));

// const deleteContact = createAsyncThunk("contacts/deleteContactStatus", (id, token) =>
//   api.deleteContact(API_ENDPOINT, id, token)
// );

// export { getContacts, addContact, deleteContact };